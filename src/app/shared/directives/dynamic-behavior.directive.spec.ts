import { DynamicBehaviorDirective } from './dynamic-behavior.directive';

describe('DynamicBehaviorDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicBehaviorDirective();
    expect(directive).toBeTruthy();
  });
});
